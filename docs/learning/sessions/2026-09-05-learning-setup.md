# 学习体系重整与导师工作流初始化

- 日期: 2026-09-05.
- 类型: 学习管理, 不是 Session 2, 未开始 U1 正式教学.
- 来源: 当前用户对话, Session 1 笔记, 本地保存的训练配置/日志/checkpoint 信息.

## 用户确认的背景与目标

CV 从业者, 简单学过 RL 原理, 缺少项目实战. 不熟悉 Microduck RL 工程结构, 希望先系统理解模块与运行过程. 希望导师跟踪学习内容, 自动保存记录, 给出下一步建议, 且换电脑后可通过项目文件接续.

## 已发生的实践与观察

用户提供并运行过的命令:

```bash
uv run play Mjlab-Velocity-Flat-MicroDuck --checkpoint-file logs/rsl_rl/velocity/2026-09-03_23-57-23_velocity/model_4998.pt
```

导师核对本地文件后确认:

- run 从 `2026-09-02_23-14-36_velocity/model_999.pt` 续训; 本次配置为 8192 环境, `max_iterations=4000`, 最后 checkpoint 为 `model_4998.pt`.
- checkpoint 保存的 `common_step_counter=120000`; 最后日志显示头部范围和平滑/偏差惩罚已达最终课程值. 不能解释成“5000 轮课程未完成”.
- 用户观察为仿真 play 中“头部姿势不自然”. 指令范围/奖励定义是待验证解释, 未最终确认根因, 也未验收步态质量.
- 导师做过 CPU/BAM 固定命令短测, 不等于重现了用户的 GPU play 条件, 不能记为用户完成部署排演. 临时脚本不作为跨机器复现实验的正式产物.

用户随后明确搁置头部问题, 转向学习体系建设. 后续不要自动恢复这项调试.

## 本次决定与产物

- 将四周阅读清单重整为六单元: 项目地图, 机器人仿真, 环境交互, 训练管理, 小实验, 部署排演.
- 前三单元优先; 受控实验放在理解环境数据流之后, 不是马上追加训练.
- 不重复跑入门长训练. 采用概念/职责 → 小段源码 → 小验证 → 记录的方式.
- 修改“新增 actor 维度”练习, 保持共享 61D 约定; 区分迭代与环境步, 不承诺固定训练耗时.
- 建立项目 skill, [学习入口](../README.md), [计划](../plan.md), [进度](../progress.md). Session 1 保留; 其中指标解释随复习按源码校正.

## 理解与实践状态

本次没有测验或新练习, 不提升掌握等级. 用户训练/play 已记录为实践; PPO/GAE 等仍是“已讲解, 待验证理解”.

## 下次接续

Session 2, U1.1/U1.2. 以 velocity 训练命令为入口, 对照 `pyproject.toml`, `train_cli.py`, `tasks/__init__.py` 建立职责表和启动链. 不需要 GPU 或新训练. 本次停在学习体系建设, 等用户继续学习.

## 本次建设的验证

- skill 已安装在 `.agents/skills/microduck-mentor/`, 根目录 `AGENTS.md` 与中文镜像已加入学习请求入口.
- skill 格式校验通过; 31 个本地 Markdown 引用全部存在; 新学习文件和 skill 未绑定本机绝对路径.
- 新文件未被 Git 忽略; `git diff --check` 通过. 本次为文档和 skill 变更, 未启动 RL 训练或回归测试.
- 文件已写入本地工作区, 尚未提交或推送. 跨电脑前仍需同步这些变更. 未触碰原有训练代码改动.
